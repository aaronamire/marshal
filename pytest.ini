[pytest]
markers =
    inference: marks tests that require the inference server (deselect with -m "not inference")
    integration: marks slow integration tests that may span multiple components (deselect with -m "not integration")
