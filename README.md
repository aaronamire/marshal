# Leaves OS

An AI-native operating system where agents are first-class primitives.

## Current Status: Phase 0 — Agent Core

The intent-based agent loop. Natural language in, actions out, everything audited.

## Quick Start
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python leaves.py
```

## Architecture

See docs/ for full architecture documentation.
